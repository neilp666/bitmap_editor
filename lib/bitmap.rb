class Bitmap
  def initialize(width, height)
    @width = width.to_i
    @height = height.to_i
    @pixels = generate_pixels(@width, @height)
  end

  def generate_pixels(width, height)
    
  end

end
